# .streamlit/secrets.toml.example
#
# Copy this to .streamlit/secrets.toml for LOCAL development,
# OR paste these key=value pairs into the Streamlit Cloud secrets editor
# (App settings → Secrets).
#
# NEVER commit secrets.toml to git.

# ── Required ──────────────────────────────────────────────────────────────────
# Supabase connection string — find it in:
#   Supabase project → Settings → Database → Connection string → URI
#   Use the "Session mode" (port 5432) URI for direct connections.
DATABASE_URL = "postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# ── Optional (leave blank to disable) ────────────────────────────────────────
OPENAI_API_KEY    = ""
ANTHROPIC_API_KEY = ""
NEWS_API_KEY      = ""
